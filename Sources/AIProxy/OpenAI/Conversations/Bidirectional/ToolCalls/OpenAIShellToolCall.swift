//
//  OpenAIShellToolCall.swift
//  AIProxy
//
//  Created by Lou Zell on 12/21/25.
//
// OpenAPI spec: FunctionShellCallItemParam, version 2.3.0, line 65956
// OpenAPI spec: FunctionShellCall, version 2.3.0, line 65424
// Encodable: https://platform.openai.com/docs/api-reference/conversations/create#conversations_create-items-item-shell_tool_call
// Decodable: https://platform.openai.com/docs/api-reference/conversations/list-items-object#conversations-list_items_object-data-shell_tool_call
//
// These may need to be split, as the decodable has a `created_by` field that the encodable does not.
// For now I'll make all fields required, as I find it unlikely that a developer will need to craft an instance themselves.
// It's more likely that a decoded shell tool call is passed back into the model at a future time.

/// A tool representing a request to execute one or more shell commands.
nonisolated public struct OpenAIShellToolCall: Codable, Sendable {
    /// The shell commands and limits that describe how to run the tool call.
    public let action: OpenAIShellToolCallAction

    /// The unique ID of the shell tool call generated by the model.
    public let callID: String

    /// The unique ID of the shell tool call. Populated when this item is returned via API.
    public let id: String

    /// The status of the shell call.
    ///
    /// One of `in_progress`, `completed`, or `incomplete`.
    public let status: Status

    /// The type of the item. Always `shell_call`.
    public let type = "shell_call"

    /// The ID of the entity that created this tool call.
    public let createdBy: String


    private enum CodingKeys: String, CodingKey {
        case action
        case callID = "call_id"
        case id
        case status
        case type
        case createdBy = "created_by"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(action, forKey: .action)
        try container.encode(callID, forKey: .callID)
        try container.encode(type, forKey: .type)
        try container.encode(id, forKey: .id)
        try container.encode(status, forKey: .status)
        try container.encode(createdBy, forKey: .createdBy)
    }
}

extension OpenAIShellToolCall {
    /// The status of the shell call.
    public enum Status: String, Codable, Sendable {
        case inProgress = "in_progress"
        case completed
        case incomplete
    }
}
